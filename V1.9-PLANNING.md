# v1.9 规划：命令面板 + 能力组合模式

**设计灵感来源**：[ponytail](https://github.com/DietrichGebert/ponytail) 的强度级别机制

**核心思路**：bridge 从"自动探测外栈"转向"手动配置能力组合 + 持久化强度级别"，类似 ponytail 的 `/ponytail [lite|full|ultra|off]` 命令面板。

---

## 1. 问题定义

### 1.1 当前 v1.8 设计的局限

**v1.8-1/2 现状**：
- bridge 是"导航员"：`probe` 探测外栈 skill（openspec / matt / superpowers）是否存在
- 探测逻辑复杂且脆弱：依赖磁盘特征文件（`openspec/`、`.mattskills/`、git 历史）
- 用户无法**手动覆盖**探测结果：即使知道要用 matt，也必须等 bridge 探测完

**用户需求（原话）**：
> 手动设置用什么流程开发 openspec / superpowers / mattskills 或者多选。
> 这样其实探测是否存在就可以去掉了，只需要根据对话场景和上下文还有上一次用的能力，指引用户用哪个能力。

**设计矛盾**：
- 探测逻辑是为了"零配置"（ADR 铁律）
- 但用户实际工作流是"固定技术栈"（一个 repo 长期用 matt 或 openspec，不会中途换）

---

## 2. 设计目标（v1.9）

### 2.1 命令面板：`bridge mode [preset]`

参考 ponytail 的 `/ponytail [lite|full|ultra|off]`，提供 4 个预设强度：

| 强度级别 | 启用能力 | 说明 |
|---|---|---|
| `full` (默认) | memory + navigator + builtin | 全功能：个人/团队 memory + 导航推荐 + builtin 兜底模板 |
| `memory` | memory only | 只启用个人/团队 memory 规则（v1.8-3），不做导航推荐 |
| `navigator` | navigator only | 只做外栈推荐（如"推荐用 matt"），不建 memory、不生成 builtin 模板 |
| `off` | — | 关闭所有 bridge 自动行为，变成纯粹的 CLI 工具（只响应显式命令） |

**命令语法**：
```bash
# 查询当前模式
node bridge.mjs mode

# 设置模式
node bridge.mjs mode full
node bridge.mjs mode memory
node bridge.mjs mode navigator
node bridge.mjs mode off

# 模式写入 ~/.config/spec-bridge/config.json（或项目根 .bridge-config.json）
# 每轮入口例程（SKILL.md §1）读取该配置决定行为
```

### 2.2 能力栈手动配置：`bridge stacks [list|set|add|remove]`

**彻底去掉自动探测**（删除 `cmd-probe.mjs` 的 detectStack 逻辑），改为手动声明：

```bash
# 查看当前 repo 已配置的能力栈
node bridge.mjs stacks list
# 输出示例：
# [
#   {"kind": "matt", "priority": 1},
#   {"kind": "superpowers", "priority": 2}
# ]

# 设置能力栈（覆盖式）
node bridge.mjs stacks set matt,superpowers
node bridge.mjs stacks set openspec

# 增加/删除能力栈
node bridge.mjs stacks add openspec
node bridge.mjs stacks remove matt

# 配置写入 .bridge.yaml（repo 级别）或 ~/.config/spec-bridge/config.json（全局默认）
```

**三种能力栈**：
- `openspec`：使用 openspec-* 系列 skill（openspec-propose / openspec-new-change）
- `matt`：使用 matt-skills（to-spec / implement）
- `superpowers`：使用 superpowers（tdd / execute）
- `builtin`：使用 bridge 内置的 5 件模板（proposal/design/tasks/spec/contract）

**优先级规则**：
- 多选时按 priority 顺序推荐（如"优先推荐 matt to-spec，fallback 到 superpowers tdd"）
- `builtin` 永远是最低优先级兜底（除非 mode=off）

### 2.3 持久化生效（类似 ponytail）

**配置文件位置**：
```
~/.config/spec-bridge/config.json    # 全局默认
<repo>/.bridge-config.json           # 项目级别覆盖
```

**配置示例**：
```json
{
  "mode": "full",
  "stacks": [
    {"kind": "matt", "priority": 1},
    {"kind": "superpowers", "priority": 2}
  ],
  "lastUsedStack": "matt"
}
```

**入口例程读取顺序**（SKILL.md §1 修改）：
```
① 定位项目根 → node bridge.mjs layout <root>
② 读取配置
   - <root>/.bridge-config.json（项目级）
   - ~/.config/spec-bridge/config.json（全局默认）
③ node bridge.mjs list <root>
④ 根据 mode + stacks 决定行为：
   - mode=off：跳过所有自动推荐
   - mode=navigator：只推荐外栈，不建 memory/builtin
   - mode=memory：只建 memory，不推荐外栈
   - mode=full：全功能（memory + navigator + builtin 兜底）
⑤ navigator 推荐时按 stacks[] 顺序：
   "建议使用 matt to-spec（priority 1），也可用 superpowers tdd（priority 2）"
```

---

## 3. v1.9 三个 change 规划

### 3.1 v1.9-1-command-panel

**范围**：命令面板 + 持久化配置

**产物**：
- 新增 `cmd-mode.mjs`（查询/设置 mode）
- 新增 `cmd-stacks.mjs`（管理能力栈配置）
- 修改 `bridge.mjs`：入口例程读取 config.json
- 修改 `SKILL.md §1`：入口例程加"读取配置"步骤
- 测试：mode-*.test.mjs / stacks-*.test.mjs（6-8 个测试）
- ADR-0014：command-panel-and-stacks.md

**Implementation Decisions**：
- D1：配置文件格式（JSON vs YAML）→ 选 JSON（ponytail 用 JSON，且 Node.js 内置支持）
- D2：项目级 vs 全局配置优先级 → 项目级覆盖全局
- D3：mode 切换是否需要重启 AI 会话 → 不需要（每轮重算）
- D4：stacks 为空时的默认行为 → fallback 到 builtin（保持零配置）

### 3.2 v1.9-2-remove-auto-detect

**范围**：删除自动探测逻辑

**产物**：
- 删除 `cmd-probe.mjs` 的 `detectStack()` 函数（保留 probe 命令的其他功能）
- 修改 `SKILL.md §4.5`：probe 不再"探测外栈"，改为"读取配置推荐"
- 修改 probe 相关测试（probe-detect-*.test.mjs → 改为读配置）
- 清理 v1.8-1 的探测特征文件逻辑（`openspec/`、`.mattskills/` 检查）
- ADR-0015：remove-auto-detect-rationale.md

**breaking change**：
- v1.8 的 `bridge probe` 会自动探测并写入 `.bridge.log`
- v1.9 的 `bridge probe` 只读配置，不探测磁盘

**迁移路径**：
- 用户首次用 v1.9 时，如果没有 config.json，提示："未检测到能力栈配置，请运行 `bridge stacks set <kind>`"
- 或：提供自动迁移工具 `bridge migrate-v1.8-probe`，把旧的探测结果写入新配置

### 3.3 v1.9-3-context-aware-navigator

**范围**：上下文感知导航（记住上次用的能力栈）

**产物**：
- 修改 navigator 逻辑：优先推荐 `lastUsedStack`（config.json 中记录）
- 新增 `bridge stacks last <kind>`：记录本次使用的能力栈
- 修改 SKILL.md §4.5：导航推荐加"上次使用"提示
  - 例："上次你用的是 matt，本次建议继续用 matt to-spec"
- 测试：context-aware-*.test.mjs（3-4 个测试）
- ADR-0016：context-aware-navigator.md

**Implementation Decisions**：
- D1：lastUsedStack 作用域 → repo 级别（不同 repo 可能用不同栈）
- D2：多久不用就清空 lastUsedStack → 不清空（除非用户手动换）
- D3：AI 如何"记住上次" → 通过 CLI 显式写入（不依赖对话历史）

---

## 4. 与 ponytail 的对比

| 维度 | ponytail | spec-bridge v1.9 |
|---|---|---|
| **命令入口** | `/ponytail [lite\|full\|ultra\|off]` | `bridge mode [full\|memory\|navigator\|off]` |
| **持久化** | `~/.config/ponytail/config.json` | `~/.config/spec-bridge/config.json` + `.bridge-config.json` |
| **强度级别** | 4 档（lite/full/ultra/off） | 4 档（full/memory/navigator/off） |
| **作用范围** | 全局生效（影响所有对话） | 项目级可覆盖（不同 repo 可用不同配置） |
| **子智能体** | 可通过 `PONYTAIL_SUBAGENT_MATCHER` 控制注入 | 暂不支持（v1.9 只影响主 AI 对话） |
| **卸载清理** | `node scripts/uninstall.js` | `bridge mode off` + 删除 config.json |

**核心借鉴点**：
1. **强度级别不是"功能开关"，而是"工作模式"**：用户不是在禁用功能，而是在选择"我想让 bridge 帮我做多少事"
2. **持久化配置让用户"设置一次，永久生效"**：不用每次对话都重新声明
3. **命令面板简洁**：一个命令搞定，不需要复杂的配置文件语法

---

## 5. 实施策略

### 5.1 先做 v1.9-1（命令面板）

**原因**：
- v1.9-1 是基础设施（配置读写 + mode 命令）
- v1.9-2/3 都依赖 v1.9-1 的配置系统

**用户拍板项**：
1. ✅ mode 强度级别：`full / memory / navigator / off` 是否合理？
2. ✅ stacks 能力栈：`openspec / matt / superpowers / builtin` 是否完整？
3. ⚠️ 配置文件位置：全局 `~/.config/` 还是项目根 `.bridge-config.json`？（建议：两者都支持，项目级覆盖全局）
4. ⚠️ 是否需要 `bridge init` 自动生成默认配置？（建议：不自动生成，保持零配置；只在首次 `bridge mode` 时创建）

### 5.2 v1.9-2 前确认迁移策略

**breaking change 影响**：
- v1.8 用户已有的 `.bridge.log` 记录了探测结果（如 `detected: matt, superpowers`）
- v1.9 不再探测，改为读配置 → 旧项目需要迁移

**迁移工具**（可选）：
```bash
# 扫描旧 .bridge.log，提取探测结果，写入 config.json
node bridge.mjs migrate-v1.8-probe
```

### 5.3 v1.9-3 最后做（锦上添花）

**原因**：
- context-aware 是优化体验，不影响核心功能
- v1.9-1/2 完成后用户已能正常使用，v1.9-3 可作为后续优化

---

## 6. 待拍板事项

### 用户决策点

1. **v1.9 整体方向是否认可？**
   - 从"自动探测"转向"手动配置 + 持久化"
   - 去掉 probe detectStack 逻辑

2. **mode 强度级别名称是否合理？**
   - `full / memory / navigator / off` 
   - 或者改为 `all / tracking / guide / off`？

3. **stacks 配置是否需要支持"多选 + 优先级"？**
   - 当前设计：可配置多个栈（如 `[matt, superpowers]`），AI 按优先级推荐
   - 简化方案：只允许单选（一个 repo 只能用一个栈）

4. **是否需要 v1.9-3（context-aware）？**
   - 如果 stacks 配置已固定，"上次用的"意义不大
   - 或者 context-aware 改为"记住上次的 mode"（如上次用 `memory`，本次默认还是 `memory`）

5. **v1.8-2/3 原计划是否作废？**
   - 原 `v1.8-2-personal-team-summary`（domain + consensus 三态）
   - 原 `v1.8-3-to-goal-integration`（6 段格式）
   - 是否并入 v1.9 或推迟到 v2.0？

---

## 附录：ponytail 关键设计点摘录

### 七级阶梯决策流程
```
1. 这段代码真的需要存在吗？  → YAGNI
2. 代码库里已经有了吗？      → 复用
3. 标准库能做到吗？          → 用标准库
4. 平台原生特性能做到吗？    → 用原生
5. 已安装的依赖能做到吗？    → 用依赖
6. 一行能搞定吗？            → 一行
7. 以上都不行：写最小实现
```

### 持久化机制
- 配置文件：`~/.config/ponytail/config.json`
- 运行时标记：`~/.claude/.ponytail-active`（IDE 特定）
- 环境变量：`PONYTAIL_DEFAULT_MODE`（覆盖配置文件）

### 子智能体控制
- `PONYTAIL_SUBAGENT_MATCHER` 正则表达式过滤
- 默认注入所有子智能体

---

**下一步**：等用户拍板上述 5 个决策点，然后开 v1.9-1 change。
